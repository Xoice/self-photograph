# 推进记录

## 2026-06-14 — Phase 1: 前端基础设施搭建

### 完成内容

1. **环境与配置**
   - 创建 `.env.example` (VITE_API_BASE_URL, VITE_UPLOAD_BASE_URL, VITE_BILIBILI_SPACE_URL, VITE_USE_MOCK)
   - 更新 `vite.config.ts` — 添加 `@` alias 和 `/api` proxy
   - 更新 `tsconfig.app.json` — 添加 `baseUrl` 和 `paths` 映射
   - 更新 `.gitignore` — 添加 `.env` 和 `.env.*` 忽略规则

2. **TypeScript 类型层**
   - 创建 `src/types/api.ts` — 完整业务实体类型定义
   - 包含: ApiResponse, PaginatedData, SiteConfig, GalleryCategory, GalleryWorkItem, GalleryWorkDetail, VideoItem, WorkshopSummary, WorkshopDetail, ContactSubmissionPayload, WorkshopEnrollmentPayload 等

3. **API 客户端**
   - 创建 `src/api/client.ts` — Axios 实例 + 响应拦截器 + ApiError 类
   - 创建 `src/api/site.ts` — getSiteConfig()
   - 创建 `src/api/gallery.ts` — getGalleryCategories(), getGalleryWorks(), getGalleryWorkBySlug()
   - 创建 `src/api/videos.ts` — getVideos()
   - 创建 `src/api/workshops.ts` — getWorkshops(), getWorkshopBySlug()
   - 创建 `src/api/leads.ts` — submitContact(), submitWorkshopEnrollment()

4. **Mock 数据**
   - 创建 `src/mocks/siteConfig.ts` — 站点配置 mock
   - 创建 `src/mocks/gallery.ts` — 4 个画廊作品 mock
   - 创建 `src/mocks/videos.ts` — 6 个视频 mock
   - 创建 `src/mocks/workshops.ts` — 5 个研学 + KenyaExpedition 详情 mock
   - 创建 `src/mocks/index.ts` — mock 拦截器，按 URL 返回 mock 数据

5. **自定义 Hooks**
   - 创建 `src/hooks/useSiteConfig.ts`
   - 创建 `src/hooks/useGalleryWorks.ts`
   - 创建 `src/hooks/useGalleryWork.ts`
   - 创建 `src/hooks/useGalleryCategories.ts`
   - 创建 `src/hooks/useVideos.ts`
   - 创建 `src/hooks/useWorkshops.ts`
   - 创建 `src/hooks/useWorkshop.ts`
   - 创建 `src/hooks/useSubmitContact.ts`
   - 创建 `src/hooks/useSubmitEnrollment.ts`

6. **QueryClient Provider**
   - 更新 `src/main.tsx` — 添加 QueryClientProvider，staleTime 5min

7. **依赖安装**
   - axios
   - @tanstack/react-query

### 遇到的问题与解决方案

- **TypeScript 构建错误**: 响应拦截器返回 `body.data` 导致类型不匹配
  - 解决: 改为返回 `{ ...response, data: body.data }` 保持 AxiosResponse 结构

### 验证结果

- [x] `tsc --noEmit` 无错误
- [x] `npm run build` 成功
- [x] 页面无变化（仅基础设施搭建，未改动组件）

### 下一步

Phase 2: 前端模块重构

---

## 2026-06-14 — Phase 2: 前端模块重构

### 完成内容

1. **路由重构**
   - 更新 `src/App.tsx` — 新增 `/gallery/:slug` 和 `/workshops/:slug` 路由
   - 保留 `/photographystudy/kenya-expedition` 兼容旧链接
   - Footer 动态化 (使用 useSiteConfig)

2. **新建详情页**
   - 创建 `src/pages/Gallery/WorkDetailPage.tsx` — 作品详情页，useGalleryWork + GSAP 动画 + 加载/错误态
   - 创建 `src/pages/Workshops/WorkshopDetailPage.tsx` — 研学详情页，改造自 KenyaExpedition，动态渲染 itinerary/highlights/fees

3. **首页 Section 改造**
   - `HeroSection.tsx` — 使用 useSiteConfig，动态渲染 heroTitle/heroSubtitle
   - `BioSection.tsx` — 使用 useSiteConfig，动态渲染 bioTitle/bioContent
   - `GallerySection.tsx` — 使用 useGalleryWorks，GSAP 横向滚动添加 dependencies + ScrollTrigger.refresh()
   - `VideoSection.tsx` — 使用 useVideos，URL 字段映射 (thumbnail→coverImage, duration→durationText, bilibiliUrl→videoUrl)
   - `PhotographyStudy.tsx` — 使用 useWorkshops，日期格式化 + 状态映射 + slug 跳转
   - `ContactSection.tsx` — 使用 useSiteConfig 联系信息 + React Hook Form 表单

4. **联系表单功能化**
   - 安装 react-hook-form, @hookform/resolvers, zod
   - contactSchema: name(必填), email(格式), message(>=10字符)
   - 使用 MUI TextField 替代原生 input
   - 提交按钮调用 useSubmitContact().mutate
   - 添加提交中/成功/失败状态反馈

### 遇到的问题与解决方案

- **GallerySection GSAP 横向滚动**: 数据加载后 scroller 宽度变化
  - 解决: `useGSAP` 添加 `dependencies: [works.length]`，tween 创建前检查 works 非空，末尾调 `ScrollTrigger.refresh()`

### 验证结果

- [x] `tsc --noEmit` 无错误
- [x] `npm run build` 成功 (1,718 KB)

### 文件变更汇总

**新建文件:**
- src/types/api.ts
- src/api/client.ts, site.ts, gallery.ts, videos.ts, workshops.ts, leads.ts
- src/mocks/siteConfig.ts, gallery.ts, videos.ts, workshops.ts, index.ts
- src/hooks/useSiteConfig.ts, useGalleryWorks.ts, useGalleryWork.ts, useGalleryCategories.ts, useVideos.ts, useWorkshops.ts, useWorkshop.ts, useSubmitContact.ts, useSubmitEnrollment.ts
- src/pages/Gallery/WorkDetailPage.tsx
- src/pages/Workshops/WorkshopDetailPage.tsx
- .env.example

**修改文件:**
- vite.config.ts (alias + proxy)
- tsconfig.app.json (paths)
- src/main.tsx (QueryClientProvider)
- src/App.tsx (路由 + Footer)
- src/api/client.ts (mock 拦截器)
- src/pages/Home/sections/HeroSection.tsx
- src/pages/Home/sections/BioSection.tsx
- src/pages/Home/sections/GallerySection.tsx
- src/pages/Home/sections/VideoSection.tsx
- src/pages/Home/sections/PhotographyStudy.tsx
- src/pages/Home/sections/ContactSection.tsx
- .gitignore

### 下一步

Phase 3: 后端 MVP (NestJS + Prisma + PostgreSQL)

---

## 2026-06-14 — Phase 3: 后端 MVP

### 完成内容

1. **项目结构**
   - 创建 `apps/api/` 目录结构
   - package.json, tsconfig.json, nest-cli.json

2. **Prisma Schema**
   - 创建 `prisma/schema.prisma` — 12 个数据模型
   - User, SiteConfig, GalleryCategory, GalleryTag, GalleryWork, Video, Workshop, WorkshopHighlight, WorkshopItinerary, WorkshopFeeItem, ContactLead, WorkshopEnrollment, MediaAsset

3. **核心模块**
   - `AuthModule` — login (POST /admin/auth/login), me (GET /admin/auth/me), JWT strategy, JwtAuthGuard
   - `SiteModule` — getConfig (GET /site/config), updateConfig (PUT /admin/site/config)
   - `GalleryModule` — getCategories, getWorks, getWorkBySlug + Admin CRUD
   - `VideosModule` — getVideos + Admin CRUD
   - `WorkshopsModule` — getWorkshops, getWorkshopBySlug + Admin CRUD
   - `LeadsModule` — submitContact, submitEnrollment + Admin 查看
   - `MediaModule` — upload (POST /admin/media/upload)
   - `HealthModule` — GET /health

4. **Common 组件**
   - `ResponseTransformer` — 统一响应格式 { code: 0, message: "ok", data }
   - `ApiExceptionFilter` — 统一错误格式
   - `JwtAuthGuard` — JWT 认证守卫
   - `CurrentUser` — 用户信息装饰器

5. **Seed 脚本**
   - 创建 `prisma/seed.ts` — 1个admin + 站点配置 + 画廊分类/作品 + 视频 + 研学(含行程/亮点/费用)

6. **Docker Compose**
   - 创建 `docker-compose.yml` — PostgreSQL 16

7. **环境配置**
   - 创建 `.env` — DATABASE_URL, JWT_SECRET, CORS_ORIGIN

### 遇到的问题与解决方案

- **Prisma schema 重复 where 字段**: gallery.service.ts 中 getCategories 方法有两个 where
  - 解决: 合并为一个 where 对象

### 验证结果

- [x] `npx prisma generate` 成功
- [x] `npx nest build` 成功

### 文件变更汇总

**新建文件:**
- apps/api/package.json, tsconfig.json, nest-cli.json, .env
- apps/api/src/main.ts, app.module.ts
- apps/api/src/prisma/prisma.module.ts, prisma.service.ts
- apps/api/src/common/interceptors/response.transformer.ts
- apps/api/src/common/filters/api-exception.filter.ts
- apps/api/src/common/guards/jwt-auth.guard.ts
- apps/api/src/common/decorators/current-user.decorator.ts
- apps/api/src/modules/auth/* (module, service, controller, strategy, dto)
- apps/api/src/modules/site/* (module, service, controller, admin-controller)
- apps/api/src/modules/gallery/* (module, service, controller, admin-controller)
- apps/api/src/modules/videos/* (module, service, controller, admin-controller)
- apps/api/src/modules/workshops/* (module, service, controller, admin-controller)
- apps/api/src/modules/leads/* (module, service, controller, admin-controller)
- apps/api/src/modules/media/* (module, service, controller)
- apps/api/src/modules/health/* (module, controller)
- apps/api/prisma/schema.prisma, seed.ts
- docker-compose.yml

### 下一步

Phase 4: 集成切换 (Mock → Real API)

---

## 2026-06-14 — Phase 4: 集成切换

### 完成内容

1. **Vite Proxy 配置**
   - `vite.config.ts` 已配置 `/api` proxy → `localhost:3000`

2. **Mock/Real API 切换**
   - 通过 `VITE_USE_MOCK` 环境变量控制
   - `true`: 使用 mock 数据 (默认)
   - `false`: 使用真实后端 API

3. **前端构建验证**
   - `npm run build` 成功 (1,718 KB)

### 验证结果

- [x] 前端 `npm run build` 成功
- [x] 后端 `npx nest build` 成功
- [x] Vite proxy 配置正确

### 启动说明

**前端开发服务器:**
```bash
# 使用 mock 数据 (默认)
npm run dev

# 使用真实 API
VITE_USE_MOCK=false npm run dev
```

**后端开发服务器:**
```bash
# 1. 启动 PostgreSQL
docker-compose up -d

# 2. 运行数据库迁移
cd apps/api
npx prisma migrate dev

# 3. 填充种子数据
npx prisma db seed

# 4. 启动后端
npm run start:dev
```

### 全链路验证清单

- [ ] 后端启动后 `GET /health` 返回 200
- [ ] `GET /api/v1/site/config` 返回站点配置
- [ ] `GET /api/v1/gallery/works` 返回作品列表
- [ ] `GET /api/v1/videos` 返回视频列表
- [ ] `GET /api/v1/workshops` 返回研学列表
- [ ] `POST /api/v1/leads/contact` 可提交联系表单
- [ ] `POST /api/v1/admin/auth/login` 登录返回 JWT
- [ ] 前端 `VITE_USE_MOCK=false npm run dev` 正常加载数据
- [ ] 画廊横向滚动正常
- [ ] 作品详情页 `/gallery/:slug` 正常
- [ ] 研学详情页 `/workshops/:slug` 正常
- [ ] 联系表单提交正常

### 下一步

全栈改造完成。

---

## 2026-06-14 — 移动端 + 桌面端优化 + Bug 修复

### 完成内容

1. **移动端优化**
   - Navbar 汉堡菜单 + Drawer
   - GallerySection 卡片响应式尺寸
   - CustomCursor 移动端隐藏
   - 主题 cursor:none 仅桌面端生效

2. **桌面端优化**
   - 详情页 lazy loading + Suspense
   - Navbar 下划线动画 + 箭头旋转
   - Gallery 卡片悬停效果 + overlay + scroll hint

3. **深度代码审查 + Bug 修复 (两轮)**

**CRITICAL 修复:**
| # | 问题 | 修复 |
|---|------|------|
| 1 | 登录完全不工作 | 响应拦截器返回 body.data 而非 AxiosResponse |
| 2 | Mock 空路由崩溃 | 未匹配路由返回 40002 错误 |
| 3 | Admin 新增弹窗不打开 | 使用独立 dialogOpen 状态 |
| 4 | requestAnimationFrame 内存泄漏 | 存储 ID 并 cancel |
| 5 | JWT Secret 硬编码回退 | 已知问题，部署时需配置 |
| 6 | ResponseTransformer 未注册 | 注册为全局拦截器 + 异常过滤器 |
| 7 | 研学详情联系方式为空 | 从 SiteConfig 获取 |

**HIGH 修复:**
| # | 问题 | 修复 |
|---|------|------|
| 8 | useAuth 不是 Context | 改为 AuthContext + Provider |
| 9 | 无 Error Boundary | 添加 ErrorBoundary 组件 |
| 10 | CustomCursor 内存泄漏 | 改为事件委托 |
| 11 | Admin sidebar 整页刷新 | 改用 navigate() |
| 12 | useAuth 竞态条件 | 添加 cancelled flag |
| 13 | 报名无容量检查 | $transaction + 容量检查 |
| 14 | 公开接口暴露未发布内容 | 检查 isPublished |
| 15 | SQLite 不支持 mode:insensitive | 移除该参数 |
| 16 | ScrollToTop console.log + 泄漏 | 清理并添加 cleanup |

### 文件变更汇总

**新建文件:**
- src/contexts/AuthContext.tsx
- src/components/ui/ErrorBoundary.tsx

**修改文件:**
- src/App.tsx (AuthProvider, ErrorBoundary, RAF fix)
- src/api/client.ts (响应拦截器修复)
- src/mocks/index.ts (空路由错误)
- src/hooks/useAuth.ts (已删除，移至 Context)
- src/components/ui/CustomCursor.tsx (事件委托)
- src/components/ScrollToTop.tsx (清理)
- src/pages/Admin/AdminLayout.tsx (navigate fix)
- src/pages/Admin/WorksManager.tsx (dialog fix)
- src/pages/Admin/VideosManager.tsx (dialog fix)
- src/pages/Admin/WorkshopsManager.tsx (dialog fix)
- src/pages/Admin/LoginPage.tsx (import fix)
- src/pages/Admin/DashboardPage.tsx (import fix)
- src/pages/Workshops/WorkshopDetailPage.tsx (status 中文化)
- apps/api/src/main.ts (ResponseTransformer + ExceptionFilter)
- apps/api/src/modules/leads/leads.service.ts (transaction + capacity)
- apps/api/src/modules/gallery/gallery.service.ts (isPublished + SQLite)
- apps/api/src/modules/workshops/workshops.service.ts (isPublished + contact)

### 验证结果

- [x] `npx tsc --noEmit` 前端无错误
- [x] `npm run build` 前端构建成功
- [x] `npx nest build` 后端构建成功

### 下一步

---

## 2026-06-14 — 第三轮优化

### 完成内容

1. **SEO 优化**
   - 修复 `lang="en"` → `lang="zh-CN"`
   - 添加 meta description, keywords, author
   - 添加 Open Graph 标签
   - 添加 Twitter Card 标签
   - 添加 theme-color

2. **后端 Admin DTO 验证**
   - 创建 `gallery/dto/gallery.dto.ts` — CreateWorkDto, UpdateWorkDto
   - 创建 `videos/dto/videos.dto.ts` — CreateVideoDto, UpdateVideoDto
   - 创建 `workshops/dto/workshops.dto.ts` — CreateWorkshopDto, UpdateWorkshopDto
   - 所有 admin controller 使用 DTO 替代 `any`
   - Admin list 支持分页参数 (page, pageSize)

3. **PhotographyStudy 状态芯片颜色修复**
   - registration_open → 绿色
   - full → 红色
   - 其他 → 灰色

4. **Admin 管理页面错误处理**
   - 三个管理页面添加 error state + Alert 通知
   - 加载/保存/删除失败显示友好错误提示
   - 替换 console.error 为用户可见的错误信息

### 文件变更汇总

**新建文件:**
- apps/api/src/modules/gallery/dto/gallery.dto.ts
- apps/api/src/modules/videos/dto/videos.dto.ts
- apps/api/src/modules/workshops/dto/workshops.dto.ts

**修改文件:**
- index.html (SEO meta + lang fix)
- apps/api/src/modules/gallery/admin-gallery.controller.ts (DTO + 分页)
- apps/api/src/modules/videos/admin-videos.controller.ts (DTO + 分页)
- apps/api/src/modules/workshops/admin-workshops.controller.ts (DTO + 分页)
- src/pages/Home/sections/PhotographyStudy.tsx (状态颜色)
- src/pages/Admin/WorksManager.tsx (错误处理)
- src/pages/Admin/VideosManager.tsx (错误处理)
- src/pages/Admin/WorkshopsManager.tsx (错误处理)

### 验证结果

- [x] `npx tsc --noEmit` 前端无错误
- [x] `npm run build` 前端构建成功
- [x] `npx nest build` 后端构建成功

### 下一步

---

## 2026-06-14 — 图片上传功能 + 画廊流程优化

### 完成内容

1. **后端上传基础**
   - MulterModule 配置 (磁盘存储、文件过滤、大小限制 10MB)
   - 静态文件服务 `/uploads/*`
   - 上传接口改进 (验证、错误处理)
   - 媒体列表接口 (分页查询)
   - 媒体删除接口

2. **前端上传基础设施**
   - `src/api/media.ts` — uploadMedia, getMediaList, deleteMedia
   - `src/components/ui/ImageUploader.tsx` — 点击/拖拽上传、进度条、预览、删除
   - `src/components/ui/ImageCropper.tsx` — canvas 裁剪、预设比例、拖拽调整
   - `src/components/ui/MediaBrowser.tsx` — 媒体库浏览、多选、搜索

3. **管理表单集成**
   - WorksManager: coverImage → ImageUploader
   - VideosManager: coverImage → ImageUploader
   - WorkshopsManager: coverImage → ImageUploader

4. **媒体库管理页面**
   - `src/pages/Admin/MediaPage.tsx` — 图片网格、上传、删除、复制 URL
   - 路由 `/admin/media`
   - 侧边栏导航添加"媒体库"

5. **画廊流程优化**
   - 首页画廊点击 → `/gallery` 列表页
   - 列表页点击 → `/gallery/:slug` 详情页
   - 新增"查看全部作品"按钮

### 文件变更汇总

**新建文件:**
- apps/api/uploads/.gitkeep
- apps/api/.gitignore
- src/api/media.ts
- src/components/ui/ImageUploader.tsx
- src/components/ui/ImageCropper.tsx
- src/components/ui/MediaBrowser.tsx
- src/pages/Admin/MediaPage.tsx
- src/pages/Gallery/GalleryListPage.tsx

**修改文件:**
- apps/api/src/main.ts (静态文件服务)
- apps/api/src/modules/media/media.module.ts (Multer 配置)
- apps/api/src/modules/media/media.service.ts (验证 + URL + 列表/删除)
- apps/api/src/modules/media/admin-media.controller.ts (列表/删除接口)
- src/mocks/index.ts (上传 mock)
- src/pages/Admin/WorksManager.tsx (ImageUploader)
- src/pages/Admin/VideosManager.tsx (ImageUploader)
- src/pages/Admin/WorkshopsManager.tsx (ImageUploader)
- src/pages/Admin/AdminLayout.tsx (媒体库菜单)
- src/pages/Home/sections/GallerySection.tsx (跳转 /gallery + 查看全部按钮)
- src/App.tsx (媒体库路由 + 画廊列表页路由)

### 验证结果

- [x] `npx tsc --noEmit` 前端无错误
- [x] `npm run build` 前端构建成功
- [x] `npx nest build` 后端构建成功

### 下一步

可继续优化:
- 部署到云服务器
- Gallery submenu 链接功能
- 后台管理 CRUD 完善 (分类选择器、标签管理)
- SEO 增强
