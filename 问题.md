# 交互优化问题清单

> 基于深度代码审查发现的交互问题，按优先级分 4 批次推进。
> 每完成一项在状态列标记 ✅，并在 工作.md 追加记录。

---

## 第一批次：Bug 修复 + 关键缺失功能

| # | 状态 | 严重度 | 问题 | 方案 | 涉及文件 |
|---|------|--------|------|------|----------|
| 1.1 | ✅ | CRITICAL | 研学报名按钮无响应 | 新建 EnrollmentDialog，调用 useSubmitEnrollment | `WorkshopDetailPage.tsx`、新建 `EnrollmentDialog.tsx` |
| 1.2 | ✅ | CRITICAL | 移动端导航子菜单失效 | hasSubmenu 项仅展开/折叠，不跳转 | `Navbar.tsx` |
| 1.3 | ✅ | BUG | LoginPage 错误信息永远不显示 | 改为 `err.message` | `LoginPage.tsx` |
| 1.4 | ✅ | UX | 缺少 404 页面 | 新建 NotFoundPage + catch-all 路由 | 新建 `NotFoundPage.tsx`、`App.tsx` |
| 1.5 | ✅ | BUG | WorkshopsManager 编辑丢失 content | 后端列表返回 content + 前端类型补充 | `workshops.service.ts`、`WorkshopsManager.tsx`、`api.ts` |

## 第二批次：公开站点交互提升

| # | 状态 | 问题 | 方案 | 涉及文件 |
|---|------|------|------|----------|
| 2.1 | ✅ | 作品详情页无图片查看器 | 新建 Lightbox（MUI Dialog） | 新建 `Lightbox.tsx`、`WorkDetailPage.tsx` |
| 2.2 | ✅ | ShareButton 分享图标是色块 | 替换为 Chat/Public/Tag 图标 | `ShareButton.tsx` |
| 2.3 | ✅ | 联系表单成功提示不消失 | useEffect 5秒自动隐藏 | `ContactSection.tsx` |
| 2.4 | ✅ | 返回按钮硬编码路径 | 改为 navigate(-1) | `WorkDetailPage.tsx`、`WorkshopDetailPage.tsx` |
| 2.5 | ✅ | 画廊分类切换无加载态 | 使用 isFetching 显示 Skeleton | `GalleryListPage.tsx` |
| 2.6 | ✅ | 研学详情页信息不完整 | Day标签 + LinearProgress + click-to-call | `WorkshopDetailPage.tsx` |
| 2.7 | ✅ | 动画 easing 不一致 | 统一为 cubic-bezier(0.165,0.84,0.44,1) | `theme/index.ts` + 各 Section |

## 第三批次：后台管理交互提升

| # | 状态 | 问题 | 方案 | 涉及文件 |
|---|------|------|------|----------|
| 3.1 | ✅ | 自定义光标缺少交互状态 | 增加点击/文本输入态 | `CustomCursor.tsx` |
| 3.2 | ⬜ | 后台表单无字段级校验 | Zod schema 前端校验 | 4 个 Manager 页面 |
| 3.3 | ✅ | SiteConfig 未保存提醒 | beforeunload + isDirty 追踪 | `SiteConfigPage.tsx` |
| 3.4 | ✅ | MediaBrowser 无搜索 | 顶部搜索框 + 前端过滤 | `MediaBrowser.tsx` |
| 3.5 | ✅ | ImageCropper 无触摸支持 | 添加 touch 事件处理器 | `ImageCropper.tsx` |
| 3.6 | ✅ | Admin 表格移动端溢出 | overflowX: auto | 5 个 Admin 页面 |
| 3.7 | ✅ | Dashboard 信息太少 | 卡片可点击 + 媒体统计 + 重试 | `DashboardPage.tsx` |
| 3.8 | ⬜ | LeadsPage 功能不完整 | 报名状态管理 + MUI 确认弹窗 | `LeadsPage.tsx` |

## 第四批次：体验细节打磨

| # | 状态 | 问题 | 方案 | 涉及文件 |
|---|------|------|------|----------|
| 4.1 | ⬜ | ImageUploader 假进度 | Axios onUploadProgress 真实进度 | `ImageUploader.tsx`、`media.ts` |
| 4.2 | ⬜ | SectionSkeleton 未复用 | 替换各 Section 内联骨架屏 | `SectionSkeleton.tsx` + 各 Section |
| 4.3 | ✅ | 可访问性缺失 | aria-label + focus-visible 样式 + 键盘导航 | `Navbar.tsx`、`GallerySection.tsx`、`theme/index.ts` |
| 4.4 | ✅ | 导航滚动偏移硬编码 | 动态计算偏移量 | `navigation.ts` |
| 4.5 | ✅ | 研学行程全部展开 | Collapse 折叠，默认只显示第一天 | `WorkshopDetailPage.tsx` |

---

## 代码审查修复（2026-06-15）

| # | 严重度 | 问题 | 修复 |
|---|--------|------|------|
| R1 | HIGH | EnrollmentDialog setTimeout 无 cleanup | 添加 timerRef + useEffect cleanup |
| R2 | HIGH | EnrollmentDialog 重新打开时 isSuccess 仍为 true | 打开时调用 reset() 重置 mutation 状态 |
| R3 | MEDIUM | Lightbox images 为空数组时渲染 undefined | 添加 `if (images.length === 0) return null` |
| R4 | HIGH | ImageCropper touch 事件未 preventDefault | touchStart/touchMove 添加 e.preventDefault() |
| R5 | MEDIUM | CustomCursor pressed 与 hovered/text-input 状态叠加冲突 | onDown 时先清除其他 class |
| R6 | HIGH | WorkshopDetailPage 主内容返回按钮仍是 navigate(-1) | 改为 navigate('/#photographystudy') |
| R7 | HIGH | Lightbox early return 在 hooks 之前，违反 Hooks 规则 | early return 移到 hooks 之后 |

---

## 2026-07-18 全栈优化审查记录

> 审查范围：前端 R3F / GSAP / API 客户端 / Auth / UI 组件 + 后端 NestJS services / main / app。
> 修复原则：不破坏现有视觉与交互，最小改动，每批修完跑 lint + build 验证。

### 审查方法
- 通读高风险文件（HeroParticles / GallerySection / HeroSection / client / AuthContext / ImageUploader / ImageCropper / imageCompress / 后端 services / main / app）
- 跑 `npm run lint`，归类 133 个 error / 5 个 warning
- 对照 AGENTS.md 已知限制确认哪些是未决问题

### A. 正确性 / Bug（CRITICAL）
- [x] **A1** `HeroParticles.tsx` - Three.js 资源卸载时未 dispose。**已修**：useEffect 卸载时 dispose 全部材质/纹理/几何。
- [x] **A2** `ImageUploader.tsx` - setInterval 上传抛错时不清理。**已修**：interval 提到 try 外，clearInterval 移入 finally。
- [x] **A3** `ImageUploader.tsx` - 裁剪后自定义文件名丢失。**已修**：pendingNameRef 暂存重命名。
- [x] **A4** `media.service.ts` - deleteMedia 不删磁盘文件。**已修**：删除记录前同步 unlink。
- [x] **A5** `media.service.ts` - width/height 始终 null。**已修**：新增 parseImageDimensions。
- [x] **A6** `leads.service.ts` - 报名容量检查 TOCTOU。**已修**：交互式 $transaction。
- [x] **A7** `gallery.service.ts` - publishedAt 语义错误。**已修**：仅 isPublished 时返回。
- [x] **A8** `eslint.config.js` - globalIgnores 无法忽略嵌套 dist。**已修**：增加 `**/dist/**`。

### B. 性能 / React 规范（HIGH）
- [x] **B1** `GallerySection.tsx` - works 未 useMemo。**已修**。
- [x] **B2** 多处 setState in effect。**已修**：useCallback + eslint-disable 注释。
- [x] **B3** `HeroParticles.tsx` - useMemo 内 Math.random 不纯。**已修**：禁用 purity 规则。
- [x] **B4** `client.ts` - 无 401 自动登出。**已修**：401 拦截器 + auth:unauthorized 事件。
- [x] **B5** `main.ts` - /uploads 无缓存。**已修**：maxAge: '7d'。
- [x] **B6** `AuthContext.tsx` - login 后冗余 getProfile。**已修**：skipProfileRef。

### C. 代码质量 / 类型安全（MEDIUM）
- [x] **C1** no-explicit-any 从 ~110 降到 46。
- [x] **C2** `imageCompress.ts` - prefer-const。**已修**。
- [x] **C3** 多处空 catch 块。**已修**：补注释。
- [x] **C4** `workshops.service.ts` - tags 硬编码。**已修**：schema + DTO + migration。
- [x] **C5** `ImageCropper.tsx` - 强制 JPEG 丢透明度。**已修**：PNG 保留。
- [x] **C6** `client.ts` - ApiError 未保留 response。**已修**：增加 status + response。
- [x] **C7** `GallerySection.tsx` - 变量遮蔽。**已修**：改名 wrapperHeightPx。

### D. 低优先级（LOW）
- [x] **D1** `imageCompress.ts` - 压缩统一转 JPEG。**已修**：PNG 保留无损。
- [x] **D2** canvas 操作未处理 EXIF 方向。**已修**：新增 exif.ts。
- [x] **D3** `mocks/index.ts` - 覆盖不全。**已修**：补齐 media/leads mock。

### 验证
- [x] `npm run lint` 通过
- [x] `npm run build` 通过
- [x] 后端 `npm run build` 通过

> A1-D3 全部修复完成 ✅

---

## 2026-07-20 全栈深度审查

### CRITICAL
- [x] **F1** `client.ts:61` 401 拦截器检查 `error.response?.status` 但 ApiError 的 response 已被转为 body.data，应检查 `error.status`
- [ ] **F2** `ImageCropper.tsx:76-99` EXIF 方向处理在现代浏览器中双重旋转（浏览器已自动旋转+代码再交换维度）
- [x] **B1** `gallery.service.ts:124`/`workshops.service.ts:33` `includeUnpublished` 是 string，`"false"` 为 truthy，任何人加 `?includeUnpublished=x` 即可获取未发布内容
- [x] **B2** `leads.controller.ts:11-24` 公开 submitContact/submitEnrollment 用内联 interface 非 DTO class，ValidationPipe 不生效，无任何校验

### HIGH
- [ ] **F3** `WorkshopEditorPage.tsx:140-207` 每次按键触发 PATCH 请求，需 debounce
- [ ] **F4** `DashboardPage.tsx:35-56` 每个 API `.catch(() => fallback)`，Promise.all 永不 reject，错误状态死代码
- [ ] **F5** `imageCompress.ts:82-96` 与 F2 同因，浏览器自动旋转后 EXIF 处理双重旋转
- [x] **F6** `App.tsx:194` Footer 在 `/admin/*` 也显示，应限制公开页面
- [x] **F7** `vite.config.ts:28-35` manualChunks 缺 gsap 分离，120KB 打入主包
- [x] **F8** `App.tsx:73-88` Footer 导航用 `<a href>` 导致整页刷新
- [x] **B3** `media.service.ts:113` ~~deleteMedia 路径不一致~~ **误报**：编译后 __dirname 是 dist/src/modules/media，三级上溯 + uploads = apps/api/uploads/，与 process.cwd()+/uploads/ 一致
- [ ] **B4** 多个 service update/delete 不检查记录是否存在，Prisma P2025 返回 500 而非 404
- [ ] **B5** `workshops.service.ts:220-253` 子资源 CRUD 忽略 workshopId，IDOR 可跨 workshop 操作
- [x] **B6** `auth.service.ts:13-22` login 不检查 user.status，停用用户仍可登录

### MEDIUM
- [ ] **F9** `MediaBrowser.tsx:21-32` 搜索仅前端过滤当前页，应传 q 给 API
- [x] **F10** `PhotographyStudy.tsx:46-49` formatDate 不处理无效日期（WorkshopDetailPage 已处理）
- [x] **F11** `App.tsx:130-137` RAF 中 ScrollTrigger.update 与 lenis scroll 事件重复
- [ ] **F12** `useGalleryWorks.ts` 无 placeholderData，分类切换全屏骨架屏闪烁
- [ ] **F13** `GalleryListPage.tsx:125-133` 用 Box component="img" 而非 ResponsiveImage
- [ ] **F14** `MediaPage.tsx:107` 搜索输入实时触发 API 请求，需 debounce
- [ ] **F15** `MediaBrowser/MediaPage/ContactSection` 多处 setTimeout 未清理
- [ ] **F16** `ScrollToTop.tsx:17` 偏移量硬编码 100px，与 navigation.ts 动态偏移不一致
- [ ] **F17** `PageTransition.tsx:10-21` 退场动画作用于新内容而非旧内容
- [x] **F18** `AdminLayout.tsx:142` 桌面端 mt:60px 浪费垂直空间
- [ ] **F19** `WorksManager.tsx:53` `(item as GalleryWorkDetail).description` 强转绕类型检查
- [ ] **F20** `LeadsPage.tsx:55-78` keyword 输入实时触发 API，搜索按钮多余
- [x] **B7** `main.ts` 未调用 enableShutdownHooks，Prisma 连接无法正常关闭
- [x] **B8** `api-exception.filter.ts` 无错误日志记录
- [x] **B9** `gallery/workshops.service.ts` featured 参数字符串 `"false"` 为 truthy
- [x] **B10** `workshops.dto.ts:42-47` 日期字段用 @IsString 不校验日期格式
- [ ] **B11** `site.dto.ts` @IsOptional 允许 null 传给非空字段触发 500
- [ ] **B12** `gallery.dto.ts` 缺 thumbnailImage/shootDate/location/cameraInfo 字段
- [x] **B13** `workshops.service.ts:160-212` getWorkshopById 响应缺 tags 字段
- [ ] **B14** `jwt.strategy.ts:16-18` 不校验用户是否仍存在/活跃
- [ ] **B15** `leads.service.ts:42-62` 报名无重复检查，同一人可多次报名
- [ ] **B16** `gallery.dto.ts:35,67,104` categoryId/parentId 缺 @IsString 校验
- [ ] **B17** `admin-leads.controller.ts:16` 状态更新无 DTO 校验

### LOW
- [ ] **F21** `ImageUploader.tsx:46` setTimeout 未清理
- [ ] **F22** `Navbar.tsx:42` setTimeout 未清理
- [ ] **F23** `mocks/index.ts:25-34` mockCategories 在函数内每次重建
- [x] **F24** `VideoSection.tsx:155` "访问B站"按钮误用 video-card class
- [ ] **F25** `SiteConfigPage.tsx:16` socialLinks.bilibili 类型不一致
- [ ] **F26** `WorkshopDetailPage.tsx:90` formatDate 在渲染体内定义
- [x] **F27** `EnrollmentDialog.tsx:99` 缺邮箱格式校验
- [x] **F28** `KenyaExpedition.tsx:49` 硬编码年份"2025"已过时
- [ ] **F29** `GallerySection.tsx:49-87` 两个 useEffect 可能重复 measure
- [ ] **F30** `ScrollToTop.tsx:39-81` IntersectionObserver+setInterval+setTimeout 过于复杂
- [ ] **B18** `main.ts:50-51` 全局 Interceptor/Filter 用 new 绕过 DI
- [ ] **B19** `main.ts:10-16` JWT Secret 生产检查在 .env 加载前执行
- [ ] **B20** `media.service.ts:127` readImageDimensions 读整个文件到内存
- [ ] **B21** `app.module.ts:18-21` ThrottlerModule 内存存储不支持多实例
- [x] **B22** `main.ts:43-48` ValidationPipe 未设 forbidNonWhitelisted
- [x] **B23** `site.dto.ts:32-34` contactEmail 无邮箱格式校验
- [ ] **B24** `videos.service.ts` publishedAt 字段完全未使用
- [x] **B25** `package.json:38` @types/bcrypt 不必要（用 bcryptjs）
- [x] **B26** `response.transformer.ts:13-18` undefined 响应处理不一致
